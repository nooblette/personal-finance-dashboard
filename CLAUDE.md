# 프로젝트 지침

## 작업 단위 커밋
- **각 작업이 끝날 때마다 별도 커밋**을 생성한다. 여러 작업을 한 커밋에 묶지 않는다.
- 사용자가 명시적으로 부탁하지 않아도 자동으로 커밋한다 (이 지침이 standing authorization 역할).
- 커밋 메시지는 한국어 기준 "type: 한국어 요약" 형식 (`feat:`, `fix:`, `refactor:`, `docs:`, `chore:` 등)을 따른다. 최근 히스토리 참고.
- 한 작업에서 여러 파일/영역이 바뀌어도, 그 작업 단위로는 하나의 커밋이다 (작업 != 파일).
- 작업 도중 발견한 사소한 무관한 변경은 별도 커밋으로 분리.

## 미리보기/브라우저
- 로컬 dev 미리보기는 cmux 내장 브라우저로 확인. playwright MCP는 사용하지 않는다.
- `npm run dev`는 `--host` + HTTPS(@vitejs/plugin-basic-ssl) 구성으로 LAN 노출이 기본.
